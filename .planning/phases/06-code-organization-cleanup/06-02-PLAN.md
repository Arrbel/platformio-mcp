---
phase: 06-code-organization-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/tools/upload.ts
  - tests/port-resolution.test.ts
  - tests/phase-a-semantics.test.ts
autonomous: true
requirements:
  - CORG-02

must_haves:
  truths:
    - "uploadFirmware and uploadAndMonitor share a common execution helper"
    - "Validation, environment resolution, status classification, and result shaping are not duplicated"
    - "Public behavior of uploadFirmware remains unchanged"
  artifacts:
    - path: "src/tools/upload.ts"
      provides: "Shared upload execution helper"
      contains: "executeUploadRun"
  key_links:
    - from: "uploadFirmware"
      to: "executeUploadRun"
      via: "shared helper call"
      pattern: "executeUploadRun"
---

<objective>
Remove the duplicated upload flow between `uploadFirmware` and `uploadAndMonitor` by introducing a shared internal execution helper.

Purpose: Lower maintenance cost in the upload path before future execution-layer growth makes the duplication harder to unwind.
Output: One shared helper in `src/tools/upload.ts` used by both public upload variants.
</objective>

<context>
@.planning/STATE.md
@src/tools/upload.ts
@tests/port-resolution.test.ts
@tests/phase-a-semantics.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce a shared upload execution helper</name>
  <files>src/tools/upload.ts</files>
  <action>
    1. Extract the shared validation, environment resolution, arg construction, execution, classification, and result shaping logic into one internal helper.
    2. Parameterize only the target list (`upload` vs `upload + monitor`) and monitor-port coupling behavior.
    3. Keep the current public function signatures unchanged.
  </action>
  <verify>
    <automated>cd E:/program/platformio-mcp && npm test -- --run tests/port-resolution.test.ts tests/phase-a-semantics.test.ts</automated>
  </verify>
  <done>The upload module contains one shared helper and both public functions route through it without regression.</done>
</task>

</tasks>

<verification>
- `npm test -- --run tests/port-resolution.test.ts tests/phase-a-semantics.test.ts` exits 0
- `npx tsc --noEmit` exits 0
- duplicated validation/resolution blocks are removed from `src/tools/upload.ts`
</verification>

<success_criteria>
Upload logic duplication is materially removed while preserving the current public behavior of the upload APIs.
</success_criteria>

<output>
After completion, create `.planning/phases/06-code-organization-cleanup/06-02-SUMMARY.md`
</output>
