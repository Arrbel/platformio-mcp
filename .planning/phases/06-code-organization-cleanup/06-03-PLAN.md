---
phase: 06-code-organization-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - src/index.ts
  - package.json
  - tests/version.test.ts
autonomous: true
requirements:
  - CORG-03

must_haves:
  truths:
    - "The MCP server version comes from package.json rather than a hardcoded string"
    - "A test proves the reported runtime version matches package.json"
    - "No duplicate version literal remains in index.ts"
  artifacts:
    - path: "src/index.ts"
      provides: "Runtime version sourcing from package metadata"
    - path: "tests/version.test.ts"
      provides: "Version consistency verification"
  key_links:
    - from: "src/index.ts"
      to: "package.json"
      via: "imported package metadata version"
      pattern: "version"
---

<objective>
Remove the hardcoded MCP server version and derive it from `package.json`, backed by an explicit consistency test.

Purpose: Eliminate the last confirmed small but real maintenance divergence in the entrypoint.
Output: `src/index.ts` uses package metadata for version, and `tests/version.test.ts` locks that behavior.
</objective>

<context>
@.planning/STATE.md
@src/index.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive MCP server version from package metadata</name>
  <files>src/index.ts, tests/version.test.ts</files>
  <action>
    1. Replace the hardcoded version literal in `src/index.ts` with a value sourced from package metadata.
    2. Add a focused test asserting the runtime-reported version source matches `package.json`.
    3. Keep startup behavior otherwise unchanged.
  </action>
  <verify>
    <automated>cd E:/program/platformio-mcp && npm test -- --run tests/version.test.ts</automated>
  </verify>
  <done>Server version is sourced from package metadata and a test protects the behavior.</done>
</task>

</tasks>

<verification>
- `npm test -- --run tests/version.test.ts` exits 0
- `npm run build` exits 0
- `src/index.ts` contains no hardcoded semantic version literal
</verification>

<success_criteria>
Phase 6 ends with organized types, deduplicated upload flow, and runtime version consistency derived from package metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/06-code-organization-cleanup/06-03-SUMMARY.md`
</output>
