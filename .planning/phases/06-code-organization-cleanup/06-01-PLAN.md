---
phase: 06-code-organization-cleanup
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 5 complete
files_modified:
  - src/types.ts
  - src/types/common.ts
  - src/types/board.ts
  - src/types/device.ts
  - src/types/project.ts
  - src/types/build.ts
  - src/types/upload.ts
  - src/types/monitor.ts
  - src/types/library.ts
  - src/types/tool-contract.ts
autonomous: true
requirements:
  - CORG-01

must_haves:
  truths:
    - "Type definitions move into domain files behind a barrel export"
    - "All existing imports from ../types.js or ../../types.js continue to work"
    - "No runtime schema or type contract changes are introduced during the split"
  artifacts:
    - path: "src/types.ts"
      provides: "Barrel re-export preserving the old import path"
    - path: "src/types/monitor.ts"
      provides: "Monitor domain types and schemas"
  key_links:
    - from: "src/types.ts"
      to: "src/types/*.ts"
      via: "re-export barrel"
      pattern: "export \* from"
---

<objective>
Split the monolithic `src/types.ts` into domain-focused modules while preserving the existing `src/types.ts` import surface as a barrel.

Purpose: Reduce monolithic type maintenance cost without creating a cross-repo churn event.
Output: Domain files under `src/types/` and a thin `src/types.ts` re-export layer.
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@src/types.ts
@src/tools/
@tests/
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create domain type modules and barrel exports</name>
  <files>src/types.ts, src/types/*.ts</files>
  <action>
    1. Create domain files for common, board, device, project, build, upload, monitor, library, and tool-contract concerns.
    2. Move the existing interfaces and schemas into those files with minimal changes.
    3. Convert `src/types.ts` into a barrel that re-exports all domain modules so no current imports need to change in the same phase.
    4. Preserve all export names exactly.
  </action>
  <verify>
    <automated>cd E:/program/platformio-mcp && npx tsc --noEmit</automated>
  </verify>
  <done>`src/types.ts` is a barrel file and the project still compiles without import-path breakage.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `npm test -- --run` exits 0
- `src/types.ts` contains only re-exports and no monolithic definitions
</verification>

<success_criteria>
The type system is organized by domain while the existing `src/types.ts` import surface remains backward compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/06-code-organization-cleanup/06-01-SUMMARY.md`
</output>
