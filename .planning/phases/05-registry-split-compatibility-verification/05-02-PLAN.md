---
phase: 05-registry-split-compatibility-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/tools/registry.ts
  - src/tools/definitions/init-project.ts
  - src/tools/definitions/build-project.ts
  - src/tools/definitions/clean-project.ts
  - src/tools/definitions/upload-firmware.ts
  - src/tools/definitions/start-monitor.ts
  - src/tools/definitions/search-libraries.ts
  - src/tools/definitions/install-library.ts
  - src/tools/definitions/list-installed-libraries.ts
autonomous: true
requirements:
  - INTF-03

must_haves:
  truths:
    - "All remaining MCP tools live in per-tool definition modules"
    - "registry.ts becomes a pure aggregator with imports and array assembly only"
    - "invokeRegisteredTool and createToolRegistry retain their existing public signatures"
  artifacts:
    - path: "src/tools/registry.ts"
      provides: "Pure aggregator registry module"
      contains: "import"
    - path: "src/tools/definitions/start-monitor.ts"
      provides: "Standalone monitor tool definition"
  key_links:
    - from: "src/tools/registry.ts"
      to: "src/tools/definitions/start-monitor.ts"
      via: "aggregated tool definition import"
      pattern: "definitions/start-monitor"
---

<objective>
Complete the registry split by extracting the remaining execution and library tool definitions so `registry.ts` contains only imports, aggregation, and the `createToolRegistry` / `invokeRegisteredTool` exports.

Purpose: Remove the confirmed architectural hotspot while preserving every MCP-visible tool contract.
Output: One definition file per MCP tool and a thin `registry.ts` aggregator.
</objective>

<context>
@.planning/STATE.md
@src/tools/registry.ts
@src/tools/build.ts
@src/tools/upload.ts
@src/tools/monitor.ts
@src/tools/libraries.ts
@tests/execution-meta.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract remaining execution and library tool definitions</name>
  <files>src/tools/registry.ts, src/tools/definitions/*.ts</files>
  <action>
    1. Create definition modules for the remaining tools:
       - `init_project`
       - `build_project`
       - `clean_project`
       - `upload_firmware`
       - `start_monitor`
       - `search_libraries`
       - `install_library`
       - `list_installed_libraries`
    2. Move the exact definition objects and handlers into those modules.
    3. Keep all strings, summary rules, warning logic, and meta calculation semantics identical.
    4. Update `registry.ts` so it contains only imports, an aggregated `toolRegistry` array, and the exported `createToolRegistry` / `invokeRegisteredTool` functions.
  </action>
  <verify>
    <automated>cd E:/program/platformio-mcp && npm test -- --run tests/execution-meta.test.ts tests/registry.test.ts</automated>
  </verify>
  <done>All tool definitions live outside registry.ts and registry tests still pass.</done>
</task>

</tasks>

<verification>
- `src/tools/registry.ts` contains no inline tool definition objects
- `npm test -- --run tests/execution-meta.test.ts tests/registry.test.ts` exits 0
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
Every MCP tool definition has its own module and `registry.ts` is reduced to an import-and-aggregate shell without public API changes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-registry-split-compatibility-verification/05-02-SUMMARY.md`
</output>
