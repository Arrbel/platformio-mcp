---
phase: 05-registry-split-compatibility-verification
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - tests/registry-compatibility.test.ts
  - tests/registry.test.ts
autonomous: true
requirements:
  - INTF-04

must_haves:
  truths:
    - "Compatibility verification proves MCP tool input schemas did not change during the split"
    - "Tool names and schema payloads are validated as a stable contract"
    - "Registry split is considered done only after no-regression verification passes"
  artifacts:
    - path: "tests/registry-compatibility.test.ts"
      provides: "Backward-compatibility coverage for registry tool contracts"
      contains: "inputSchema"
  key_links:
    - from: "tests/registry-compatibility.test.ts"
      to: "src/tools/registry.ts"
      via: "createToolRegistry() contract assertions"
      pattern: "createToolRegistry"
---

<objective>
Add an explicit compatibility verification layer for the registry split so Phase 5 closes with evidence that MCP tool input schemas and response envelopes remain backward compatible.

Purpose: The split is only safe if it is behavior-preserving, not just structurally cleaner.
Output: A compatibility-focused test file covering tool names, stable input schemas, and representative response structure invariants.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/tools/registry.ts
@tests/registry.test.ts
@tests/execution-meta.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add registry compatibility verification tests</name>
  <files>tests/registry-compatibility.test.ts</files>
  <action>
    1. Create `tests/registry-compatibility.test.ts`.
    2. Assert the full ordered tool name list from `createToolRegistry()`.
    3. Assert representative `inputSchema` objects for all current MCP tools remain identical to the current public contract.
    4. Reuse existing mock-driven invocation style where helpful, but keep this file focused on backward compatibility rather than internal implementation details.
  </action>
  <verify>
    <automated>cd E:/program/platformio-mcp && npm test -- --run tests/registry-compatibility.test.ts tests/registry.test.ts tests/execution-meta.test.ts</automated>
  </verify>
  <done>Compatibility test suite proves stable tool names and input schemas after the split.</done>
</task>

</tasks>

<verification>
- `npm test -- --run tests/registry-compatibility.test.ts tests/registry.test.ts tests/execution-meta.test.ts` exits 0
- `npm run build` exits 0
- `npm run lint` exits 0
</verification>

<success_criteria>
Phase 5 ends with structural registry cleanup and explicit evidence that MCP-facing tool contracts did not regress.
</success_criteria>

<output>
After completion, create `.planning/phases/05-registry-split-compatibility-verification/05-03-SUMMARY.md`
</output>
