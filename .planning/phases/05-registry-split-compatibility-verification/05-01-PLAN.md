---
phase: 05-registry-split-compatibility-verification
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 4 complete
files_modified:
  - src/tools/registry.ts
  - src/tools/definitions/shared.ts
  - src/tools/definitions/list-boards.ts
  - src/tools/definitions/get-board-info.ts
  - src/tools/definitions/list-devices.ts
  - src/tools/definitions/inspect-project.ts
  - src/tools/definitions/list-environments.ts
  - src/tools/definitions/doctor.ts
autonomous: true
requirements:
  - INTF-03

must_haves:
  truths:
    - "Registry extraction starts with read-oriented tools and shared helpers, not a single risky big-bang rewrite"
    - "createToolRegistry still returns the same tool names in the same order for the extracted subset"
    - "Shared helpers such as createToolResponse and withExecutionMeta are moved out of registry.ts"
  artifacts:
    - path: "src/tools/definitions/shared.ts"
      provides: "Shared ToolDefinition helpers for definition modules"
    - path: "src/tools/definitions/list-boards.ts"
      provides: "Standalone tool definition module"
    - path: "src/tools/registry.ts"
      provides: "Aggregator importing extracted definitions"
  key_links:
    - from: "src/tools/registry.ts"
      to: "src/tools/definitions/shared.ts"
      via: "imported helper utilities"
      pattern: "definitions/shared"
---

<objective>
Establish the registry split pattern with minimal risk: extract shared registry helpers and the read-oriented tool definitions first, while preserving tool order and behavior.

Purpose: De-risk the larger split by proving the per-tool definition module pattern on the least mutation-heavy tools before touching build/upload/monitor/library definitions.
Output: `src/tools/definitions/` directory with shared helpers and the first batch of extracted definitions; `registry.ts` continues to export the same public API.
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@src/tools/registry.ts
@tests/registry.test.ts
@tests/execution-meta.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared registry types and response helpers</name>
  <files>src/tools/registry.ts, src/tools/definitions/shared.ts</files>
  <action>
    1. Create `src/tools/definitions/shared.ts` and move these reusable concerns out of `registry.ts`:
       - `ToolDefinition`
       - `createToolResponse`
       - `withExecutionMeta`
    2. Keep signatures unchanged so existing tool definitions can be moved without semantic drift.
    3. Update `registry.ts` to import these helpers from the shared module.
  </action>
  <verify>
    <automated>cd E:/program/platformio-mcp && npx tsc --noEmit</automated>
  </verify>
  <done>Shared helper module exists and `registry.ts` imports from it cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Extract first-batch read-oriented tool definitions</name>
  <files>src/tools/registry.ts, src/tools/definitions/*.ts</files>
  <action>
    1. Create standalone definition modules for:
       - `list_boards`
       - `get_board_info`
       - `list_devices`
       - `inspect_project`
       - `list_environments`
       - `doctor`
    2. Each module exports a single tool definition object using the shared `ToolDefinition` type.
    3. Replace the inline objects in `registry.ts` with imports and array aggregation, preserving the current order.
    4. Do not change any input schema objects, descriptions, summaries, warning text, or nextActions wording.
  </action>
  <verify>
    <automated>cd E:/program/platformio-mcp && npm test -- --run tests/registry.test.ts tests/execution-meta.test.ts</automated>
  </verify>
  <done>Read-oriented tools live in their own modules and registry tests still pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `npm test -- --run tests/registry.test.ts tests/execution-meta.test.ts` exits 0
- `src/tools/registry.ts` no longer contains inline definitions for the extracted read-oriented tools
</verification>

<success_criteria>
The registry split pattern is established with shared helpers and a first batch of per-tool definition modules, without changing public behavior or tool ordering.
</success_criteria>

<output>
After completion, create `.planning/phases/05-registry-split-compatibility-verification/05-01-SUMMARY.md`
</output>
